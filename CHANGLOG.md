### feat: webpack（2026-08-14 20:53:45 +0800）

本次提交内容较杂，核心是完成一次项目目录重构与 Webpack 打包学习。将原先位于 `基本算法/最小堆`、`基本算法/树`、`字典树` 等中文目录下的代码迁移并重命名到 `JS/Heap`、`JS/Tree/BinayTree`、`JS/Tree/字典树` 等更规整的英文路径下。新增了 `Heap.js`，完整实现最小堆和最大堆，包含上浮、下沉、入堆、出堆等操作，并附带测试代码。同时搭建了 Webpack 示例项目，包括配置文件、HTML 模板、CSS 样式和构建产物，验证 CSS/HTML 加载及打包流程；还加入了函数柯里化示例和 AI 整理 changelog 的占位脚本。整体工作质量较好，目录结构清晰，但提交中包含 `dist` 构建产物和 lock 文件，存在可优化空间。后续可扩展 dev server、多入口打包、代码分割以及更规范的 `.gitignore` 配置。

---

### feat: 滑动窗口（2026-08-14 20:53:20 +0800）

本次提交实现了两道滑动窗口算法题：`最多K个重复元素的最长子数组` 和 `每个字符最多出现两次的最长子字符串`。两者均使用 `Map` 维护窗口内元素频率，当新加入元素导致频率超过限制时，通过移动左指针收缩窗口，并实时更新最大长度。第二题是第一题的 k=2 特例，代码结构重复但清晰，便于理解滑动窗口模板。工作质量较高，逻辑正确，注释简洁，适合作为基础模板复用。可扩展方向包括：抽象成一个通用函数，支持任意阈值；对字符题改用长度为 26 的数组优化空间复杂度；补充更多边界测试用例，如空数组、全部重复元素等。

---

### feat: 递归（2026-08-14 20:52:21 +0800）

本次提交包含三个二叉树递归相关文件：`二叉树的直径` 使用 DFS 自底向上计算，通过全局变量记录最大路径边数，返回子树高度，实现正确且高效；`合并二叉树` 递归合并两棵树的节点值，处理了空节点情况，代码简洁；`不同的二叉搜索树2` 仅创建了 TreeNode 构造函数并初始化数组，尚未完成核心生成逻辑，属于未完成状态。整体递归思路较好，但质量不均，其中直径和合并题目可以正常使用，而不同二叉搜索树需要补全。建议后续完成该题目的具体实现，并增加统一测试，可扩展至更多树形动态规划、递归回溯类题目。

---

### feat: 双堆法（2026-08-14 20:51:58 +0800）

本次提交使用双堆法实现数据流中位数类 `MedianFinder`，通过最大堆保存较小一半、最小堆保存较大一半，并在 `addNum` 中平衡两堆规模，保证差值不超过 1。`findMedian` 根据堆大小返回堆顶或平均值，代码逻辑清晰，附带了 4、5、9、10 依次插入的测试，输出结果正确。工作质量较高，是经典面试问题，模板价值大。可扩展方向包括：支持删除元素、滑动窗口中位数、使用内置优先队列简化代码，以及增加随机数据对拍测试和边界处理（如单个元素、负数、大量重复值）以提升鲁棒性。
## feat: npm git_ai（2026-08-15 04:01:31 +0800）

本次提交构建了 `ai_git` 工具：将原有 `ai_comsume.js` 重构到 `script/ai_git/src/`，调用 DeepSeek API 读取 `origin/master..HEAD` 的提交差异，生成 Markdown 总结并追加写入 changelog；同时新增 Rollup 配置，打包出 CJS/ESM 双格式产物，删除 Fast-NodeServer 子模块。还补充了滑动窗口算法注释和按位异或题目实现。整体模块划分清晰，但 `ai_changlog` 仍为空壳、构建产物提交进仓库可优化。后续可扩展为 Git hook 自动触发、多轮对话整理、接入更多模型或补充单元测试。

## feat: python learn（2026-08-15 08:40:42 +0800）

**工作内容**：新增 `python/base/data_struct.py` 数据结构占位文件；调整 `request_test.py` 请求体变量名 `payload` → `data`；新增 5 道 LeetCode Hot100 题解：LC1 两数之和、LC560 和为 K 的子数组、LC49 字母异位词分组、LC128 最长连续序列、LC283 移动零。

**可扩展**：可补充 `data_struct.py` 的实际数据结构实现，继续完成 Hot100 剩余题目，并增加单元测试验证边界情况。

**工作质量**：整体算法思路基本正确，主要案例可运行，但细节质量中等：LC1 使用 `nums.index` 在重复值场景下易返回错误索引；LC49 空数组处理不符合题意且残留调试 `print`；LC128 排序解法为 O(n log n)，不满足 O(n) 要求；LC283 倒序 `pop` 效率较低，建议改用双指针。后续需加强边界处理与复杂度优化。
## feat: 最长重复子数组 不错！（2026-08-17 07:43:25）

- **做了哪些工作**：本次提交新增 3 道数组/动态规划题解：`有多少小于当前数字的数字.js` 用排序 + Map 记录每个数字首次出现位置；`最长连续递增序列.js` 编写了暴力跳跃优化，但未完成动态规划版本；核心文件 `最长重复子数组.js` 用二维 DP 从末尾反向填表，正确求解最长公共子数组长度。
- **可以扩展**：可继续练习最长公共子序列、编辑距离；小于当前数字可用计数排序优化到 O(n)；连续递增序列可补全 DP 或滑动窗口解法。
- **工作质量**：核心 DP 思路正确，但作者花了不少时间才找到从末尾对比的突破口；另外两个文件存在未使用变量或逻辑不完整，整体中上，需要清理与补全。

---

## feat: js 多维数组创建（2026-08-17 07:42:53）

- **做了哪些工作**：本次提交新增 `JS/Array/多维数组创建.js`，演示了二维数组用 `new Array().fill().map()` 生成，三维数组用 `Array.from` 多层嵌套生成，并注释说明 `Array.from` 和 `map` 均可使用。
- **可以扩展**：可补充通用多维数组工厂函数、避免 `fill` 导致的引用共享问题、懒初始化、深拷贝以及性能对比，为后续动态规划中 DP 表的创建提供工具支持。
- **工作质量**：示例简洁实用，注释基本清晰，属于合格的基础笔记；但覆盖面较小，未涉及易错点和更多创建方式，质量中等。

---

## feat: 前缀和（2026-08-16 20:40:01）

- **做了哪些工作**：本次提交整理前缀和算法：将 `test.js` 改名为 `前缀和.js` 并补充注释；新增 `区域和检索_数组不可变.js`，用前缀和实现 O(1) 区间查询；新增 `和为K的子数组.js`，使用前缀和 + 哈希表统计，正确初始化 `map{0:1}`，并说明滑动窗口不适用；新增 `扩展.md` 记录延伸问题。
- **可以扩展**：可继续实现最短/最长子数组、元素和至多 k、所有子数组长度之和、元素和为奇数等衍生题，进一步掌握前缀和思想。
- **工作质量**：核心算法实现正确，注释清晰，能够解释滑动窗口的局限性；扩展题仅列题目未解答，是后续很好的练习方向，整体质量较高。
## feat: 4道题，找出最大的几近缺失整数和组合总数4不错（2026-08-18 06:34:26 +0800）

本次提交完成 4 道算法题：新增 JS 版“找出最大的几近缺失整数”，按 k=1、k=n 和一般情况分类处理；新增 Python 题解：最小爬楼梯花费（LC 746）、变体爬楼梯2、组合总数4（LC 377）三道动态规划题；`data_struct.py` 补充固定长度列表创建及 tuple/dict/set 占位注释。整体核心 DP 思路清晰，动态规划方程基本正确，但爬楼梯2 的步长费用递推可读性较差，需进一步验证边界；JS 变量命名和文件末尾换行可优化。可扩展：补充测试用例、复杂度分析，完善数据结构示例。
```md
## feat: 完全二叉树（2026-08-19 14:15:39 +0800）

**做了什么**：新增完全二叉树与满二叉树的定义、叶子节点公式；实现 `countNodes` 利用左右子树高度快速统计节点数；新增 AVL 定义与判断完全二叉树的层序遍历思路；删除空文件 `基本算法/双指针/test.js`。

**可扩展**：可补全判断完全二叉树与 AVL 的实际可运行代码，为 `countNodes` 增加测试用例，并修正时间复杂度为更准确的 O(log² n)。

**工作质量**：概念注释和算法思路较清晰，节点数实现能利用完全二叉树性质；但部分文件只有注释，缺少实际实现和测试，整体偏文档化，质量中等。
```
## Merge branch 'master'（2026-08-20 12:33:42 +0800）

该提交合并远程 master 分支，主要同步 `.github` 下自动生成的代码统计文件：更新深色、浅色及默认主题的 `loc-history-*.svg` 折线图，以及 `loc_history.json` 数据。新增 5 条 2026-08-19 的代码行数记录，LOC 从 59018 增至 59192。工作内容属于仓库统计可视化维护，数据一致、时间刻度刷新正确，质量良好，但属于自动化产物，无业务代码变更。可扩展为按语言/模块统计，或纳入 CI 自动更新并生成趋势 badge。
## commit 5a1d366b00519a3f50290208b2b0cfc14c376d52: feat: 轮转数组和除了自身以外数组的乘积  
**时间：2026-08-20 12:32:49 +0800**

本提交主要完成了数组相关算法题：JS/Python 双语言实现轮转数组，采用反转法避免暴力超时；除自身以外数组的乘积使用左右乘积数组实现；合并区间先按起点排序再线性合并；同时补充 Python 列表排序、插入、删除等基础操作。算法思路清晰，注释较充分。可扩展增加边界测试、不可变返回版本及性能测试。质量整体良好，但合并区间首个元素处理略冗余，建议用 `continue` 跳过首项。
# Update LOC graph [skip ci] (2026-08-20 00:36:25 UTC)

本次提交由 GitHub Actions 自动完成，更新了仓库的 LOC（代码行数）历史图表：三个 SVG 文件（dark/light/默认）及 `loc_history.json`。新增了 6 条 8 月 19 日的提交记录，LOC 从 59018 增长至 59192（+174 行）。图表数据点和横轴时间标签同步调整。未涉及业务代码，属于维护性自动化提交。可扩展方向：增加代码行数变化率、按文件类型/目录细分统计等。工作质量：自动化流程稳定，数据准确，格式规范，但无实质功能变更。
## Commit f927918 - 2026-08-19 14:15:39 +0800：完全二叉树与AVL基础

本次提交聚焦树结构，新增完全二叉树概念说明、高效节点数算法及AVL定义，删除无用文件。具体工作：实现利用完全二叉树性质计算节点数的函数（时间复杂度O(log² n)），添加完全二叉树与满二叉树注释，引入平衡二叉树AVL的概念，并删除空的`基本算法/双指针/test.js`。可扩展：实际编码实现广度优先判断完全二叉树，补充AVL旋转与平衡调整操作。工作质量：节点数算法正确，注释清晰；但判断完全二叉树仅留注释未实现，功能完整性略有欠缺。
## commit fca5e1a - Merge branch 'master' of algorithm  
**时间：** 2026-08-19 13:53:54 +0800

**做了哪些工作：**  
本次是合并远程 `master` 分支的维护性提交，主要同步了 `.github` 下的代码行数统计资产：更新 `loc-history-dark.svg`、`loc-history-light.svg`、`loc-history.svg` 三个历史趋势图，并在 `loc_history.json` 中追加 5 条新记录，LOC 从 58962 增至 59018。

**可扩展：**  
可将统计扩展为按语言、目录或模块维度展示；关联每次 commit message 自动生成说明；或通过 GitHub Actions 定时更新图表，减少手动合并和冲突。

**工作质量：**  
变更范围清晰，无功能性代码改动，图表与 JSON 数据一致，合并结果正常。作为自动化统计同步提交质量合格，但技术复杂度较低。
```markdown
# feat: shopee面试+ 最长回文子串（2026-08-19 13:53:32 +0800）

工作：新增 Shopee 面试题与最长回文子串实现。`JS/面试.js` 包含数组组合、最长回文子串、三角形计数；`基本算法/双指针/` 下新增单结果和多结果中心扩散法。可扩展：补全回溯逻辑，三角形计数从 O(n³) 优化到 O(n²)，补充测试用例。质量：多结果版较完整；单结果版 `max_len` 未定义会报错，面试题一未完成，总体健壮性和完整度待提升。
```
## feat: 单调栈-每日温度（2026-08-19 12:19:26 +0800）

**提交哈希：** `8a75293b337088deb8f0b393441d624c512c5384`

**工作总结：**  
本次提交新增了「每日温度」问题的 JavaScript 实现，位于 `基本算法/单调栈/每日温度.js`。算法使用单调递减栈保存温度索引，遍历时若当前温度高于栈顶索引对应温度，则弹出栈顶并计算天数差 `i - top` 存入结果数组。整体思路符合单调栈经典模板，时间复杂度 O(n)，空间复杂度 O(n)，代码简洁且注释清楚说明了弹出条件与原因。工作质量较好，但缺少测试用例，文件末尾无换行符，建议补充边界情况（如空数组、连续升温）测试。可扩展方向包括：循环数组场景（如 LeetCode 503）、返回更高温度的具体数值而非天数，或复用模板解决“下一个更大元素”类问题。
# feat: python hot100 - 2026-08-19 09:16:41 +0800

**工作内容**：新增三道 Hot100 题解：最大子数组和（动态规划）、盛最多水的容器（双指针）、三数之和（排序+暴力+set 去重）；同时修复 `data_struct.py` 中遍历列表时误写 `item[i]` 的问题，并补充切片示例。

**可扩展**：三数之和可改为排序+双指针，优化至 O(n²) 并移除调试 `print`；最大子数组和可压缩 DP 空间为 O(1)；容器题可去掉循环外重复计算，使代码更简洁。

**工作质量**：动态规划与双指针思路正确，数据修复到位；但三数之和当前实现存在超时风险和调试残留，整体质量中等。
## 259cf80f Update LOC graph [skip ci] - 2026-08-19 00:36:36 UTC

**做了什么**：该提交由 GitHub Actions 自动生成，更新了 `.github` 下的三份 LOC 历史趋势 SVG 图与 `loc_history.json`。数据新增 5 条提交记录，LOC 从 58962 更新至 59018，并重新计算了横轴日期刻度与曲线末端数据点。

**可扩展**：可为趋势图增加提交事件标记、异常波动告警、CI 摘要评论，或将 LOC 数据接入项目看板做长期可视化。

**工作质量**：作为自动维护类提交，数据同步准确、图表格式一致，无业务代码风险；但属于元数据更新，人工审查价值有限。
```md
## feat: Node fs和path（2026-08-19 00:56:53 +0800）

**Commit:** `4602c4a3e68d9cc4b1aa51d56c6ba3643f00f561`

**做了哪些工作：**  
本次提交围绕 Node.js 核心模块学习，新增 `Path/path.js` 演示 ESM 下通过 `import.meta.filename/dirname` 获取路径，新增 `process.js` 展示 `cwd/env/argv` 用法；调整 `readFile.js`，增加 utf8 读取与 JSON 序列化示例；删除冗余的 `write.md` 与旧 `import.js`。

**可以扩展什么：**  
可继续补充路径拼接、异常处理、Buffer 读取、环境变量配置和命令行参数解析等实战内容，并加入可运行测试或断言。

**工作质量：**  
笔记结构清晰，注释较完整，但部分代码片段例如 `hostjs_path` 未定义，示例不够完整，建议补全为可独立运行的最小示例。
## commit 162ad50acb9bc129276d337dc160c61bd6e10641：Merge branch 'master'（2026-08-18 21:47:30 +0800）

这是一次 master 分支合并提交，主要同步了 `.github` 下的代码行数历史统计：`loc_history.json` 新增 5 条提交记录，LOC 从 58828 增至 58962；同时更新了暗色、亮色及默认 SVG 曲线图的时间轴刻度和曲线末端。整体属于项目统计元数据的常规同步，没有直接业务代码改动。

可扩展方向：将统计图生成与提交接入 CI 自动完成，减少人工 merge 冲突；增加月度趋势、语言分布等统计维度。

工作质量合格：图表与 JSON 数据保持一致，合并正确；但自动生成文件的人工同步价值有限，且 `loc-history.svg` 与 `loc-history-light.svg` 内容存在冗余，后续可统一简化。
## feat: 矩阵DFS（2026-08-18 21:43:51 +0800）

**提交哈希：** `9645bb61555ac6d1ee7428ed4e9d8055f5a635a0`

本次提交新增 `总价值可以被K整除的岛屿数目.js`，通过DFS遍历连通岛屿、累加总价值并判断能否被K整除；将 `dfs.js` 重命名为 `树的前中后序遍历.js`，使文件名语义更清晰；并清理了 `前序遍历构造二叉搜索树.js` 中的多余空行。

可扩展为BFS/并查集方案，补充空网格、单行/单列等边界测试，移除调试 `console.log`、统一代码风格。整体逻辑正确，DFS去重与边界处理基本有效，但存在调试输出残留、测试不足和风格不一致，质量中上，需继续打磨。
## feat: kmp next数组（2026-08-18 17:43:04 +0800）

**Commit:** `f9270fb277bdacfa6b86a3f3063f1347eccf0a12`

本次新增 `基本算法/字符串搜索/最长快乐前缀.js`，实现 `longestPrefix` 函数，通过 KMP next 数组求字符串的最长相同前后缀，最终用 `next[n]` 截取快乐前缀。工作内容清晰，将 KMP 思想应用到具体问题，代码简洁、算法正确。可扩展方向包括：补充空串、单字符、重复字符等边界测试；将 next 数组生成逻辑抽离为通用工具，便于复用到字符串匹配等场景；添加复杂度说明和更详细注释。整体质量较好，注释概括了题意，但缺少测试与边界说明。