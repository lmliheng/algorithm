模拟退火算法

初始状态：随机一个解，设定一个较高温度。
邻域搜索：在当前解附近随机生成一个新解。
接受准则​：若新解更优 → 直接接受。若新解更差 → 以一定概率接受，概率由 Metropolis 准则决定
降温:T*=alph

函数：
一维多峰函数f(x) = sin(x) + 0.1 * x^2验证全局搜索能力，如果T 太小 / alpha 太大 会卡在正半轴局部最优
正确的情况是在全局谷底

一维f(x) = x^2 ，正常情况是不管哪个点作为初始点，结果都接近于0

二维 f(x,y)=x * x + y * y