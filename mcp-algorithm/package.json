{
  "name": "mcp-algorithm",
  "version": "0.1.0",
  "private": true,
  "description": "Agent-driven MCP server: query my algorithm solutions by natural language, return file paths with GitHub links",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.ts",
    "smoke": "node --env-file-if-exists=.env src/smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
