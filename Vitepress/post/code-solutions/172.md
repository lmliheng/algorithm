# 题目 172

:::code-group

```javascript [172.js]
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
let n = 30;
let sum = 1;
for (let i = 1; i <= n; i++) {
    sum *= i;
}
console.log(sum);
let ZeroNum = 0;
let sum_str = sum.toString();
for (let i = sum_str.length - 1; i >= 0; i--) {
    if (sum_str[i] === '0') {
        ZeroNum++;
    }
    else {
        break;
    }
}
console.log(ZeroNum);
```

```typescript [172.ts]
   let n=30
   
   let sum =1
    for(let i=1;i<=n;i++){
        sum*=i
    }
    console.log(sum)

    let ZeroNum=0
    let sum_str=sum.toString()
    for(let i=sum_str.length-1;i>=0;i--){
        if(sum_str[i]==='0'){
            ZeroNum++
        }else{
            break
        }
    }

    console.log(ZeroNum)
```

:::

## 源码地址

- [JavaScript](https://github.com/lmliheng/algorithm/blob/master/JS/172.js)
- [TypeScript](https://github.com/lmliheng/algorithm/blob/master/JS/172.ts)
