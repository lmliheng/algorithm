# 题目 202

:::code-group
`javascript [202.js]
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
let n = 2;
let set = new Set();
let res = n;
while (true) {
    let str = res.toString();
    res = 0;
    console.log("str=", str);
    for (let i = 0; i < str.length; i++) {
        console.log("str[i]=", str[i]);
        res += (+str[i]) * (+str[i]);
    }
    console.log("res=", res);
    if (res === 1) {
        console.log(true);
        break;
    }
    if (set.has(res)) {
        console.log(false);
        break;
    }
    else {
        set.add(res);
    }
}

`
`	ypescript [202.ts]
let n = 2

let set = new Set()
let res = n
while (true) {
    let str = res.toString()
    res = 0
    console.log("str=", str)
    for (let i = 0; i < str.length; i++) {
        console.log("str[i]=", str[i])
        res += (+str[i]) * (+str[i])
    }
    console.log("res=", res)
    if (res === 1) {
        console.log(true)
        break
    }

    if (set.has(res)) {
        console.log(false)
        break
    } else {
        set.add(res)

    }

}
:::

## 源码地址

- [JavaScript](https://github.com/lmliheng/algorithm/blob/master/JS/202.js)
- [TypeScript](https://github.com/lmliheng/algorithm/blob/master/JS/202.ts)
