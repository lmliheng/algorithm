# 题目 34

:::code-group
`javascript [34.js]
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
let nums = [5, 7, 7, 8, 8, 10];
let target = 6;
let first_index = nums.indexOf(target);
if (first_index !== -1) {
    for (let i = first_index; i < nums.length; i++) {
        if (target !== nums[i]) {
            console.log([first_index, i - 1]);
        }
    }
}
else {
    console.log([-1, -1]);
}

`
`	ypescript [34.ts]
   let nums=[5,7,7,8,8,10]
   let target=6
   let first_index=nums.indexOf(target)
    if(first_index!==-1){
        for(let i=first_index;i<nums.length;i++){
            if(target!==nums[i]){
                console.log([first_index,i-1])
            }
        }

    }else{
        console.log([-1,-1])
    }
:::

## 源码地址

- [JavaScript](https://github.com/lmliheng/algorithm/blob/master/JS/34.js)
- [TypeScript](https://github.com/lmliheng/algorithm/blob/master/JS/34.ts)
