# 题目 2648

:::code-group
`javascript [2648.js]
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function* fibGenerator() {
    let a = 0;
    let b = 1;
    while (true) {
        yield a;
        [a, b] = [b, a + b];
        // a=b
        // b=a+b
    }
}
;
const fib = fibGenerator();
console.log(fib.next().value); // 5
console.log(fib.next().value); // 10
console.log(fib.next().value); // 15
console.log(fib.next().value); // undefined

`
`	ypescript [2648.ts]
function* fibGenerator(): Generator<number, any, number> {

    let a = 0
    let b = 1
    while (true) {
        yield a;
        [a,b]=[b,a+b]
        // a=b
        // b=a+b
    }

};
const fib = fibGenerator()
console.log(fib.next().value)// 5
console.log(fib.next().value)// 10
console.log(fib.next().value)// 15
console.log(fib.next().value)// undefined

:::

## 源码地址

- [JavaScript](https://github.com/lmliheng/algorithm/blob/master/JS/2648.js)
- [TypeScript](https://github.com/lmliheng/algorithm/blob/master/JS/2648.ts)
