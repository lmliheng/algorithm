# 2026-07-09 工作日志

## CSV 文件优化
- data/ 下所有 CSV 文件统一为英文逗号分隔
- Diurnal_Station.csv: 只保留磁场强度和时间两列，时间去掉小数转为整数，添加表头 `磁场强度/nT,time`
- Line01~06_Raw.csv: 表头逗号化 `距离/m,磁场强度大小/nT,time`，清除双逗号和多余空格
- Line04_Raw.csv: 从 Line04_Raw.txt（tab分隔）转换生成

## test.py 完成
- 读取改为 CSV 文件（逗号分隔），get_diurnal_value 使用 usecols=(0,1)
- 使用 Agg 后端，添加中文字体 SimHei
- 添加测线号标注（L1~L6）和正/负异常体标注
- 成功生成 style1_basic.png、style2_multipanel.png、style3_geographic.png
- Python 环境使用 managed venv: C:\Users\BBG\.workbuddy\binaries\python\envs\default

## test.py 简化：只生成第一张图
- 删除了 style2、style3 两个子图模块
- 测线编号 y 坐标改为 i*5（即 5,10,...,30），间距5米
- 纵坐标刻度 0~6，标签显示"测线编号"
- 横坐标标签改为"测线方向 (m)"
- 所有测点用 scatter 标出（黑点），并附带 ΔT 数值标注
- 右侧 L1~L6 测线号标注、正/负异常体红色/蓝色箭头标注
- 输出文件: style1_basic.png
- 纵坐标刻度改为 1~6（去掉0），移除顶部 title
- 后续迭代：去掉正/负异常标注，y 轴范围精确 5~30（无上下空白）

## 磁法高级处理：magnetic_processing.py
- IGRF-14 计算（pyIGRF 包）：D=-4.33°（与用户给的-4.34°吻合）, I=45.43°, F=49443.9nT
- GPS(29.0470032, 113.1393198), 海拔35m, 2026.5年
- 频域处理实现（FFT + 镜像延拓 + 滤波器）：
  * 向上延拓 (Upward Continuation): exp(-k*h)，高度 5/10/20m
  * 化极 (RTP): 1/theta^2 滤波器
  * 垂向一阶导数: k 滤波器
- 输出 4 张图：upward_continuation.png, reduction_to_pole.png, vertical_derivative.png, summary_all_processing.png
- pyIGRF 系数文件位置: C:\Users\BBG\.workbuddy\binaries\python\envs\default\Lib\site-packages\pyIGRF\src\igrf14coeffs.txt
- 需手动从 NOAA 拉取 igrf14coeffs.txt (SSL 在沙箱内可能不通)

## requirements.txt 更新
- 新增 pyIGRF>=1.0.0（用于 IGRF-14 磁场参数计算）
- 最终依赖列表：numpy, scipy, matplotlib, pyIGRF, pandas, geoist, gdal
